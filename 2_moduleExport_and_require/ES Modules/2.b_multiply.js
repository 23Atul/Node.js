
export var elem = 10;

export function calcProduct(a, b) {
    const pro = a * b;
    console.log(pro);
}


// either we can export each item indivisually or in form of obj

// export { elem, calcProduct };   // Single export statement
